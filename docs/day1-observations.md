The shortest case (E12) used 228 input tokens and finished in 1925 ms, while the longest case (E11) used 274 input tokens and took 4017 ms. Latency grew faster than input length because the longer document also produced more output tokens (107 versus 50). A short document is a poor estimator of model workload: both token count and wall time scale with document size, so you cannot budget a full run from one short example.
